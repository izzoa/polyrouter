---
'@polyrouter/shared': minor
'@polyrouter/control-plane': minor
---

Add the bundled, effective-dated pricing catalog (spec §7.7/§5/§8; invariant 4). A new global `model_prices` table (append-only, provider-namespaced `"<litellm_provider>:<model>"` keys, USD input/output + cache-read/write prices, capability flags, `is_free`, `source`, `valid_from`; CHECK constraints enforce `≥ 0` and `is_free ⇒ 0`) is exposed as a non-owned `pricing` port accessor and seeded on boot from a vendored LiteLLM snapshot. Pricing is sourced from LiteLLM's `model_prices_and_context_window.json`: a pure `parseLiteLlmCatalog` maps per-token costs to per-1M USD using the authoritative `litellm_provider` namespace, and an admin `refresh` can pull the live JSON through #4's SSRF guard (guarded dispatcher — not the auto-closing `guardedFetch` — with no loopback exception, 3xx rejected, an AbortSignal timeout, a streaming max-body-size cap, and body cleanup on any failure). A single `canonicalModelKey` builds keys for both the parser and `deriveModelKey` (host→family, unknown host → null) so a reseller's `gpt-4o` never inherits OpenAI's price, and prefixed LiteLLM keys (`gemini/…`, `openrouter/…`) round-trip with bare provider ids. A pure `resolveModelPrice` (in `@polyrouter/shared/server`, reusable by #11) resolves Model-own → local-free → catalog → null, where null is "price unknown" (distinct from `usage_estimated`) and cache rates are carried nullable for #11 to snapshot. All writes — seed, refresh, and manual override — flow through one advisory-locked, monotonic, manual-respecting `applyVersions` path, so history is never rewritten (a past lookup always returns the version then in effect) and an operator override is never clobbered by a later refresh. The `api/pricing` management API gates reads on a session and mutations on an admin under `MODE=selfhosted` (cloud disables them via a new `IdentityPort.isAdmin`). Backed by shared (Vitest) resolver/parser tests, control-plane service/fetch unit tests, and a real-Postgres e2e (boot-seed, effective-dated lookup, override immutability, refresh-appends-only, LiteLLM refresh, admin/self-host gating). Adds `undici` to control-plane.
