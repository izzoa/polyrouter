---
'@polyrouter/data-plane': minor
---

Add the protocol-translation module (spec §6.3, invariant 2): a single `Normalized*` IR in `data-plane/src/proxy/translate/` and pure OpenAI Chat Completions ⟷ Anthropic Messages in/out adapters (request, response, streaming) behind it, so the proxy core stays protocol-agnostic. Content-blocks-everywhere with a top-level `system` field; parsed-object tool inputs (with a raw/`inputParseError` variant for invalid model JSON, never thrown); first-class `tool_choice` + parallel-call control; usage stored as **uncached components** and converted by formula (Anthropic excludes cache tokens from input, OpenAI includes them) so cache-read/write tokens survive for cost (§7.7); canonical stop reasons with raw + stop-sequence passthrough; multimodal with `detail` and no URL fetching; streaming with the per-provider usage lifecycle (Anthropic start/delta, OpenAI terminal empty-`choices` chunk), per-block tool-JSON assembly finalized at `block_stop`, and split-frame tolerance. Anthropic's tool-result turn (results grouped into one user message, trailing text preserved) round-trips against the IR's one-result-per-`role:'tool'`-message model. Backed by a golden-file contract suite (`npm test -w packages/data-plane`) proving canonical round-trip equivalence and cross-translation with no network or DB. Provider deviations are per-adapter quirks; the module states but does not enforce the mid-stream commit policy (that is #10/#12). Consumed by the provider layer (#6) and the proxy (#10).
