/** Realistic demo corpus, re-exported onto the bundle so both the preview
 * cards and the design agent can compose with believable polyrouter data
 * (model names, providers, request-log rows, spend series). */
export {
  demoFakeOptions,
  demoProviders,
  demoAgents,
  demoModels,
  demoTiers,
  demoTierEntries,
  demoRules,
  demoBudgets,
  demoChannels,
  demoSpend,
  demoSummary,
  demoTimeseries,
  demoChartData,
  demoRequestRows,
} from '../solid/design-kit.mjs';
