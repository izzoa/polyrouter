# Build context for the production image (#22). Keep .npmrc — npm ci needs it.
node_modules
**/node_modules
**/dist
.turbo
**/.turbo
.git
.gitignore
.env
.env.*
coverage
**/coverage
*.log
.DS_Store

# Repo meta the image never needs
openspec
docs
TODOS.md
CLAUDE.md
AGENTS.md
docker-compose.dev.yml
docker-compose.yml
docker-compose.semantic.yml
install.sh
scripts
# …except the model-bake script the -semantic image variant COPYs at build time.
!scripts/bake-semantic-model.sh
README.md
