# Tasks: add-fallbacks-and-stream-safety

> Build order: ordered chain in resolve.ts → refactor #10's commit gate into a helper + chain walkers (ProxyCore) → breaker provider → ProxyService chain build (subscription-first, breaker) + served-model recording → tests. Reuses #6 breaker/classifiers, #10 commit boundary + drain, #11 recording. No schema migration.

## 1. Ordered chain resolution (data-plane, pure)

- [x] 1.1 Extend `data-plane/src/routing/resolve.ts`: add `RouteTarget = { providerId, modelId, externalModelId }` and `readonly chain: readonly RouteTarget[]` to `RouteDecision` (`chain[0]` = the existing primary). `resolveTier` collects ALL entries sorted by position into the chain (primary = position 0, still `empty_tier` if none); a direct `model:` target / explicit model → `chain = [self]`. Export `RouteTarget`.
- [x] 1.2 Update `resolve.spec.ts`: a tier yields a multi-entry chain in position order (primary first); an explicit model / model-target rule yields a single-element chain; empty tier still errors.

## 2. Breaker fix + chain walkers (data-plane)

- [x] 2.1 **Fix `data-plane/src/providers/breaker.ts` `withBreakerStream`**: when it observes an `error` event, `settle(breakerImpact(classifyStreamError(...)) ? 'trip' : 'success')` **before yielding it** (once-only guard), so the commit gate's `iterator.return()` can't settle `neutral` first. Keep the post-loop terminal-stop/truncation settle for the no-error case and the consumer-abandon `neutral`. Add a test: an overload error event → the breaker trips even when the consumer abandons right after.
- [x] 2.1b **Fix `data-plane/src/providers/http.ts` `openRequest`**: throw `CallCancelledError` immediately when `ctx.signal?.aborted` (before attaching the listener), so a signal already aborted during breaker admission / adapter build cannot still start the upstream call. Test a pre-aborted call.
- [x] 2.2 Refactor `data-plane/src/proxy/core.ts`: extract the single-attempt commit gate into `openAttemptStream(streamFactory: (signal: AbortSignal) => AsyncGenerator, client, opts) → {kind:'error',error} | {kind:'stream',frames,outcome}` — it creates the `AbortController` FIRST, then calls the factory with that signal (so the first-event timeout cancels the upstream). Keep `openStream`/`runBuffered` as thin single-provider wrappers.
- [x] 2.3 Add `fallbackEligible(err): boolean` on the RAW error — false for `CallCancelledError`/`AbortError` (client gone) and a `ProviderError` with `shouldFallback === false` (`bad_request`); true for `ProviderCircuitOpenError`, a member build failure, and a retryable `ProviderError` (incl. `unknown_model`). Map `ProviderCircuitOpenError` → 503 in `proxy-errors` (`toProxyError`).
- [x] 2.4 Add `runBufferedChain(breaker, attempts, client, request, ctx, signal) → { ok:true, wire, response, servedIndex, failures } | { ok:false, error, failures }` (RETURNS on exhaustion, never throws, so the trail reaches the recorder). `ChainAttempt = { providerId, externalModelId, buildAdapter: () => Promise<ProviderAdapter> }`; per attempt (skip if `signal.aborted`): `withBreaker(breaker, providerId, async () => { const a = await buildAdapter(); return a.chat(retargetedReq, {signal}); })` — build is INSIDE the callback so an open circuit skips before any build; on a fallback-eligible RAW error push `{ index, error }` + continue; on success `{ok:true}`.
- [x] 2.5 Add `openStreamChain(breaker, attempts, client, request, opts) → {kind:'error',error,failures} | {kind:'stream',frames,outcome,servedIndex,failures}` — per attempt (skip if aborted): `openAttemptStream((signal) => withBreakerStream(breaker, providerId, async function*(){ const a = await buildAdapter(); yield* a.chatStream(retargetedReq, {signal}); }), client, opts)` — build INSIDE the breaker generator (open circuit skips before build); a pre-commit `{kind:'error'}` that is fallback-eligible with members remaining → next; a `{kind:'stream'}` → commit (return frames+outcome, post-commit → the #10 terminal frame, no swap); exhausted → `{kind:'error'}` with the last error.
- [x] 2.6 Update `core.spec.ts`: buffered chain falls to the next on a retryable error + records `servedIndex`/`failures`; a `bad_request` and a cancellation stop the walk; a **broken later member's build failure** doesn't stop a healthy primary; stream chain falls back **pre-commit** and commits the next, but a **post-commit** failure yields a terminal frame with no further attempt.

## 3. Circuit-breaker provider

- [x] 3.1 Add a `CircuitBreaker` provider to `proxy.module.ts` (imports `RedisModule`): `new CircuitBreaker(redisStore, { fallback: new InMemoryBreakerStore() })` where `redisStore` is `RedisBreakerStore` over a **`BreakerRedis` wrapper that bounds `eval` with a short fail-fast deadline** (a slow/down Redis degrades to the in-memory store promptly, no hot-path stall — invariant 1). Provide via a token; overridable in tests.

## 4. ProxyService: build the chain, walk it, record the served model

- [x] 4.1 Extend `ProxyService.prepare`: from `decision.chain` (**configured order — NO auto-reorder**), load each `RouteTarget`'s provider + model rows (ownership-scoped) and build **lazy** `ChainAttempt`s whose `buildAdapter` does the SSRF re-gate + decrypt + #6 factory when the member is tried, **rethrowing setup failures as `ProviderError('unavailable', …)`** so they classify as eligible. Return `attempts: ChainAttempt[]` paired with `meta[]` (provider base_url/kind, model prices/isFree, ids) + the `RouteDecision` + `startedAt`/`requestChars`. Thread a disconnect `AbortController` into the buffered path (`proxy-http`) too.
- [x] 4.2 `completion` → `runBufferedChain` (handle the `{ok:false}` result); `stream` → `openStreamChain`. Record via #11 using `meta[servedIndex]` for the **served** provider/model/pricing and status **precedence**: streaming `outcome.status==='error'` → `error`, else `failures.length>0` → `fallback`, else `success`; total failure → one `error` row (primary meta, with the failure trail) + throw the mapped error. Extend `routing_reason` with the sanitized failure trail (`kind@externalModelId`). `RecordingContext` carries explicit served ids; widen `RecordOutcome.status`, **`RequestLogDraft.status`**, and `RequestRecorder` to `success|error|fallback`. Inject the `CircuitBreaker`.

## 5. Tests

- [x] 5.1 Extend the proxy e2e (stub upstream): a tier `[failing-model, gpt-4o]` where the stub 500s/429s the first model — a non-streaming request still succeeds on the second, and the RequestLog names the **served** model with `status=fallback` and a failure trail in `routing_reason`; a **streaming** request falls back pre-commit and delivers one clean stream; a **mid-stream** (post-commit) upstream error yields a terminal error frame with NO second model **and records `status=error`**. A **broken later member** (e.g. bad base_url) doesn't stop a healthy primary. An explicit model failure returns the error (no fallback). All-fail → one `status=error` row + a protocol error. Configured order is honored (a healthy position-0 serves even with a later subscription member).
- [x] 5.2 Breaker/cancellation tests (unit + e2e): the `withBreakerStream` fix — an overload error event trips the breaker even when the consumer abandons; repeated failures open a provider's circuit and the next request **skips it without an upstream call OR a `buildAdapter` call** (assert `buildAdapter` isn't invoked + the stub gets no request), served by the fallback member; an injected throwing/slow breaker store still routes via the in-memory fallback within the fail-fast deadline; a **pre-aborted signal** doesn't start the upstream and doesn't invoke a later member.

## 6. Definition of done

- [x] 6.1 `npm test -w packages/data-plane` (chain resolver + walkers), `npm test -w packages/control-plane`, and `npm run test:e2e -w packages/control-plane` (fallback + mid-stream + breaker + fallback-recording) green; `npm run build` passes; lint clean; strict TS, no `any`.
- [x] 6.2 Add a changeset (`@polyrouter/data-plane` + `@polyrouter/control-plane` minor).
- [x] 6.3 Confirm non-goals hold (no `auto` smart pipeline, no first-chunk buffer, no new provider kinds, no schema migration; the commit boundary / terminal frame / drain are #10's, reused unchanged; #6/#11 changed only additively). Update spec/deltas; leave archive-ready. **Note the ⛔ review gate: #10–12 are now the shippable core awaiting human review.**
