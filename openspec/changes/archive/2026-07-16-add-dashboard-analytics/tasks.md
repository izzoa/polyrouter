# Tasks: add-dashboard-analytics

> Build order: backend multi-value `layer` filter → analytics API client + range mapping → uPlot `<Chart>` wrapper → store Observe slices/loaders (replace the simulator; frozen window + generation guard) → Overview → Costs (+ shared range selector) → Requests + reworked decision inspector → tests → DoD. Frontend swap of the three Observe pages to #17's real API; render immutable snapshots only (invariant 4); transparency is layer-agnostic (invariant 1). One shared-type change (`decisionLayers`); new frontend dep `uplot`. No migration.

## 1. Backend — one small analytics extension (multi-value layer filter, no migration)

- [x] 1.1 `shared/server/persistence.ts`: change `AnalyticsRequestsQuery.decisionLayer?: string` → `decisionLayers?: string[]`.
- [x] 1.2 `control-plane/database/analytics.queries.ts` `listRequests`: filter `inArray(requestLogs.decisionLayer, decisionLayers)` when present (parameterized; replaces the single `eq`).
- [x] 1.3 `control-plane/src/analytics/analytics.dto.ts` + service: the `requests` DTO `layer` accepts a **comma-separated** list → `@Transform` to a trimmed non-empty `string[]` (an empty/whitespace-only segment → 400, bounded count/length); the service passes `decisionLayers`.
- [x] 1.4 Extend the analytics e2e: a two-value `layer` filter (`explicit,header`) returns exactly the matching rows across a paginated result; an empty/whitespace `layer` segment → 400.

## 2. Frontend — analytics API client + range mapping

- [x] 2.1 `frontend/src/data/api.ts`: frontend types for the #17 wire shapes (the fields the UI consumes) — `AnalyticsSummary`, `TimeseriesPoint`, `BreakdownRow`, `RequestRow` (`id`, `createdAt` **ISO string**, nullable model/provider/agent ids + labels, nullable `tierAssigned`, `decisionLayer`, `routingReason`, `status`, `escalated`, input/output tokens + nullable cacheRead/cacheWrite tokens, **all four** nullable price snapshots, nullable `cost`, `attemptCostMicros`, `durationMs`, `usageEstimated`, nullable `qualitySignal`; other safe-row fields like `priceVersionId` may be omitted as unused), `RequestsPage { rows; nextCursor }`. Add `ApiClient` methods `summary`/`timeseries`/`breakdown`/`requests` over relative `/api/analytics/*` (`credentials:'include'`), a query-string builder that omits undefined params + joins `decisionLayers` as a comma list, and the shared **`totalCostMicros(row)`** (`round((cost ?? 0)×1e6) + attemptCostMicros`) + `fmtMicros` + label-fallback helpers.
- [x] 2.2 `frontend/src/data/range.ts` (new): pure `rangeToParams(range: Range, now: number): { from: string; to: string; bucket: 'hour'|'day' }` — `24h`→(now−24h, hour), `7d`→(now−7d, day), `30d`→(now−30d, day). Unit-tested.

## 3. Frontend — uPlot chart wrapper

- [x] 3.1 `frontend/package.json`: add `uplot` (^1.6) to dependencies. Changeset notes the new dep.
- [x] 3.2 `frontend/src/components/Chart.tsx` (new): a SolidJS wrapper for a **single-series** line (requests per bucket; one scale — no dual-axis). `onMount` builds a `uPlot` into a ref div (series/axis/grid colors from CSS vars via `getComputedStyle`); a `createEffect` on `props.data` → `u.setData`; a **`createEffect` on `state.theme`** rebuilds/redraws so a toggle re-themes; `onCleanup` **disconnects the ResizeObserver AND destroys** the instance; ResizeObserver → `u.setSize`. Import `uplot/dist/uPlot.min.css` once. Props: `data` (`[secs[], counts[]]`, x = epoch seconds), label, height. (Do NOT swallow constructor errors — the Vitest setup shims `HTMLCanvasElement.getContext` instead; task 4.2/8.)

## 4. Frontend — store Observe slices + loaders

- [x] 4.1 `frontend/src/state/appState.ts`: replace the simulated Observe slices (`stats`, `chart`, `requests`, `pushLiveRequest`) with fetched state — `analyticsSummary`, `analyticsSeries: TimeseriesPoint[]`, `analyticsBreakdown: { model; provider; agent }`, `recentRequests: RequestRow[]` (Overview's unfiltered first-6), `requestList: RequestRow[]`, `requestCursor: string | null`, a frozen `requestWindow`, each with `…Loading`/`…Error`. A monotonic **`generation` guard per SHARED mutable slice** (bumped by whichever loader writes it — `loadOverview`/`loadCosts` share `analyticsSummary`+breakdowns, last-writer-wins) discards a stale response so an old-range reply can't overwrite newer state. Actions: `loadOverview()` (summary + timeseries + model breakdown + `loadRecentRequests()`), `loadCosts()` (summary + model/provider/agent breakdowns), `loadRecentRequests()` (an **unfiltered** first-6 `requests` fetch → `recentRequests`, independent of the Requests-page pagination), `loadRequests(reset)` — on `reset` snapshot `{from,to}`+filters into `requestWindow` and fetch page 1; on append reuse `requestWindow` + `requestCursor` (never re-derive the range). `setRange` reloads the active page; `setFilter` `loadRequests(true)`. On error, keep last-good data + expose the error for a retry.
- [x] 4.2 `frontend/src/App.tsx` + cleanup: drop the simulator `pushLiveRequest` 4s feed. The **aggregate** pages (Overview/Costs) load on mount + range change with a light `setInterval` refetch (~15s) gated by `live` and **cleared on unmount**; the **requests list is not polled**. **Retire the request simulator:** delete `data/simulator.ts` + `data/simulator.test.ts` (no longer used — the Observe pages are real; #20's config pages use `SEED_TIERS/RULES/LIMITS/CHANNELS`, not the request generator), and **decouple `data/catalog.ts` formatters from `RoutedRequest`** (generic number args). Remove the dead Observe seeds from `data/seed.ts`, keeping only what #20 still consumes. Add the `HTMLCanvasElement.getContext` shim to the Vitest setup so `<Chart>` mounts.

## 5. Frontend — Overview

- [x] 5.1 `frontend/src/pages/Overview.tsx`: remove `PreviewBanner`; use the shared `<RangeSelector>`; stat cards from `analyticsSummary` (spend, requests, tokens in/out, and **success / fallback / escalation** rates = `successCount`/`fallbackCount`/`escalatedCount` ÷ `requests`, guarding `requests===0`); `<Chart>` from `analyticsSeries` (requests per bucket, single series); "spend by model" = `analyticsBreakdown.model` → `BarRows` (map `BreakdownRow` → `SpendDatum`); "recent requests" = the `recentRequests` slice (unfiltered first-6) via the real row table. Loading + empty-range zero-states (no NaN) + a visible error/retry. Range-reflective headings. The providers strip stays.

## 6. Frontend — Costs (+ shared range selector)

- [x] 6.0 `frontend/src/components/RangeSelector.tsx` (new): extract Overview's inline `24h`/`7d`/`30d` control into a reusable component driving `setRange`; used on Overview AND Costs.
- [x] 6.1 `frontend/src/pages/Costs.tsx`: remove `PreviewBanner` + `SEED_*`; add `<RangeSelector>`; "Spend · {range}" (NOT "this month") + `~estimated` from `analyticsSummary`; a **free/paid/unpriced** split bar from the summary counts (percent by request count; relabel — subscription/api split deferred); spend-by-model/provider/agent = the three breakdowns → `BarRows`; loading/empty/error states. Keep the static price-snapshot integrity note; drop the "list price / saved" figure.

## 7. Frontend — Requests + decision inspector

- [x] 7.1 `frontend/src/types.ts` + `components/RequestTable.tsx`: generalize `DecisionLayer` to an arbitrary string + `RequestStatus` to `success|fallback|error`; the table renders the real `RequestRow` (time=`createdAt`, model=`modelLabel ?? modelId ?? 'unknown'`, provider=`providerLabel ?? providerId`, tier=`tierAssigned`, decided-by=`decisionLayer` chip [unknown → neutral], tokens=`inputTokens`→`outputTokens`, cost=**`totalCostMicros` total** [+ `~` when `usageEstimated`], latency=`durationMs`, status dot for success/fallback/error).
- [x] 7.2 `frontend/src/pages/Requests.tsx`: remove `PreviewBanner`; render `requestList` with a **"Load more"** (when `requestCursor` non-null → `loadRequests(false)`); filter chips → `setFilter` → `loadRequests(true)` mapping **all server-side**: `fallback`→`status=fallback`, `escalated`→`escalated=true`, `explicit`→`layer=explicit,header,default`, `auto`→`layer=structural,cascade`, `all`→none. Row count + loading/empty/error states.
- [x] 7.3 `frontend/src/components/Inspector.tsx`: rework to the real row — header (`modelLabel ?? modelId`, id, createdAt), route (agent→router·`decisionLayer`→provider·tier), **Decision** = `decision_layer` + the verbatim `routing_reason` (+ `escalated`/`qualitySignal` when present), **Usage & cost** = input/output/cache tokens + **all four** immutable price snapshots (`0`→"$0 free" vs `null`→"unpriced", kept distinct) + **served `cost`, attempt cost, and total** (`totalCostMicros`; null cost → "unpriced" ≠ `$0.00`) + the `~estimated` note, **Timing** = `durationMs` + status. Read snapshots only (invariant 4); drop the simulator `steps`/`feat`/`ttfb`/`routeMs`/`protocol`.

## 8. Tests

- [x] 8.1 `frontend/src/test/fakeClient.ts`: add `summary`/`timeseries`/`breakdown`/`requests` returning canned mutable data; the fake `requests` honors the frozen `cursor` + `status`/`escalated`/`decisionLayers` (server-side filter semantics) so pagination + filtering are testable.
- [x] 8.2 Vitest — unit: `rangeToParams`; summary→**success/fallback/escalation** rates; timeseries→chart transform (epoch seconds); requests filter→param mapping (`explicit`→`layer=explicit,header,default`, etc.); `totalCostMicros`; inspector view-model. Component (via `AppProvider`+fake): Overview/Costs show real numbers (not seeds) + a visible error/retry on a failing load; a request row opens the inspector rendering the `routing_reason`; "Load more" appends over a **frozen window** (no dupes/skips); a layer/status filter narrows via the fake; `<Chart>` smoke-mounts. **Retire `simulator.test.ts`** and update `App.test.tsx`/`appState.test.ts` for the removed simulated slices + the new canvas shim.

## 9. Definition of done

- [x] 9.1 `npm test -w packages/frontend` + `npm test -w packages/control-plane` + `npm run test:e2e -w packages/control-plane` green (incl. the multi-value `layer`-filter e2e); `npm run build` (shared → control-plane → frontend, incl. the uPlot bundle) passes; lint + format clean; strict TS, no `any`.
- [x] 9.2 DoD (§15): every request in the dashboard shows its tokens, cost, latency, model, and a **readable decision layer + reason** (the inspector) — verified by the component test + a manual check; Overview/Costs render real aggregates over the selected range.
- [x] 9.3 Changeset (`@polyrouter/shared` + `@polyrouter/control-plane` minor; frontend private, note the new `uplot` dep). Confirm invariants: analytics render the immutable snapshot cost/prices, never recomputed (4); the inspector is layer-agnostic — renders whatever `decision_layer`/`routing_reason` exists (1); only #17's secret-free safe views are shown (8). Update TODOS.md #19; archive.
