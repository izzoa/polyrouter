# Tasks: fix-proxy-ingress-and-drain

## 1. Configurable proxy bounds (E1.4 — do first; E1.1/E1.3 consume the config)

- [x] 1.1 In `packages/control-plane/src/proxy/proxy.config.ts`, register a proxy config namespace (shared `registerConfig`/`z`): `PROXY_MAX_BODY_BYTES` (`z.coerce.number().int().positive().default(10_485_760)`), `PROXY_FIRST_EVENT_TIMEOUT_MS` (`.int().positive().default(30_000)`), `PROXY_EVENT_TIMEOUT_MARGIN_MS` (`.int().min(0).default(500)`). (No separate inter-event var — core keeps its single first/inter-event bound; clink finding 5.)
- [x] 1.2 Extend `ProxyRuntime`: add `maxBodyBytes`; `firstByteTimeoutMs` = `PROXY_FIRST_EVENT_TIMEOUT_MS` (adapter bound); add `firstEventTimeoutMs` = `firstByteTimeoutMs + PROXY_EVENT_TIMEOUT_MARGIN_MS` (core's single first/inter-event bound). `loadProxyRuntime` reads the registered vars
- [x] 1.3 Unit test (`proxy.config.spec.ts`): env overrides honored; defaults preserve current values (firstByte 30_000); `firstEventTimeoutMs === firstByteTimeoutMs + margin` (so core's bound always exceeds the adapter's)

## 2. Body limit + protocol-shaped body-parser errors (E1.1)

- [x] 2.1 Add a `requestTooLarge()` helper to `packages/control-plane/src/proxy/proxy-errors.ts` (status 413, fixed message e.g. 'request body too large', type 'invalid_request_error', code 'request_too_large')
- [x] 2.2 Extract `mountBodyParsing(expressApp: express.Express, maxBodyBytes: number)` (in `mount.ts` or a small sibling module) and call it from `mountAuth` after the auth handlers. It mounts a **path-routed** parser: `req.path` under `/v1` → `express.json({ limit: maxBodyBytes })` / `express.urlencoded({ extended: true, limit: maxBodyBytes })`; otherwise the existing **default-limit** parsers — so the raised limit is confined to `/v1` and `/api` is unchanged (clink finding 2)
- [x] 2.3 In `mountBodyParsing`, mount a 4-arity `(err, req, res, next)` middleware immediately after the parsers: non-`/v1` → `next(err)`; `/v1` + `err.type === 'entity.too.large'` → render `requestTooLarge()`; `/v1` + (`err.type === 'entity.parse.failed'` or `err instanceof SyntaxError`) → render 400 `invalid request body`; via `renderProxyError(proxyErr, protocolForPath(req.path))` + `res.status().json()`, and `res.end()` when `headersSent`
- [x] 2.4 `mountAuth` reads `maxBodyBytes` from the proxy runtime/config resolved at boot and passes it to `mountBodyParsing` (value passed in — never re-loaded inside the middleware)
- [x] 2.5 Unit-test the extracted helper against a **bare Express app** (no Nest/auth) with a `/v1/echo` + an `/api/echo` route and `mountBodyParsing(app, <low limit>)` (clink finding 3): (a) a body under the limit on `/v1` → 200; (b) over the limit on `/v1/chat/completions` → 413 OpenAI-shaped JSON; on `/v1/messages` → 413 Anthropic-shaped `{type:'error',...}`; both `content-type: application/json`, no HTML/stack; (c) malformed JSON on both `/v1` routes → 400 protocol-shaped; (d) an over-limit body on `/api/echo` behaves as the default parser (not the `/v1` renderer) — proving the limit is `/v1`-scoped

## 3. Drain terminates a write-blocked stream (E1.2)

- [x] 3.1 In `packages/control-plane/src/proxy/proxy-http.ts`, change `drain(res)` → `drain(res, signal)`: race `signal`'s `'abort'` alongside `'drain'|'close'|'error'`, resolve immediately if `signal.aborted`, and have the shared `done()` remove ALL four listeners (incl. `'abort'`) to avoid listener accumulation; pass `abort.signal` at the `await drain(res)` call site
- [x] 3.2 Fix the `pumpSse` `finally` (clink finding 1): snapshot `const externallyAborted = abort.signal.aborted` **before** `abort.abort()`; after `abort.abort()` + `frames.return?.()`, do `if (externallyAborted && !res.destroyed) res.destroy(); else if (!res.writableEnded) res.end();` — a normal completion (externallyAborted false) still `res.end()`s exactly as today
- [x] 3.3 Extend `packages/control-plane/test/proxy/stream-lifecycle.e2e-spec.ts`: a client that reads NOTHING (never resumes) against the short-deadline app (`oai-bigframes` or `oai-neverend`), then `app.close()` — assert it resolves within `streamDrainDeadlineMs` + a fixed test tolerance (bounded `withTimeout`). Must FAIL against current `main` (hang) and pass after the fix. Keep the existing green backpressure/drain cases (which assert full bodies) as the no-truncation regression guard

## 4. Breaker trips on a system-imposed first-event timeout (E1.3)

- [x] 4.1 Wire the bounds in `proxy.service.ts`: pass `this.rt.firstEventTimeoutMs` (= adapter + margin) as core's `firstEventTimeoutMs` (governs first + inter-event), keep `this.rt.firstByteTimeoutMs` as the adapter bound. The margin guarantees the adapter's typed `ProviderError('unavailable', 'provider first-byte timeout')` fires before core's abort for a **pre-headers** hang
- [x] 4.2 In `packages/data-plane/src/providers/breaker.ts` `withBreakerStream` catch: make the caller-abort predicate authoritative — when `isCallerAbort` is supplied, `neutral = isCallerAbort()`, and for a non-neutral cancellation settle a **trip explicitly** (do NOT fall through to `outcomeForError`, whose `CallCancelledError → neutral` branch would re-neutralize it; clink Q1(ii)). Keep the old `isCancellation(err) || isCallerAbort?.() === true` behavior when the predicate is absent. Do NOT touch `withBreaker` (buffered) classification
- [x] 4.3 Extend `packages/data-plane/src/providers/breaker-caller-abort.spec.ts`: (a) a streaming call that throws `CallCancelledError` with `isCallerAbort = () => false` (system abort) → breaker records **trip** (repeat to threshold → opens); (b) same with `isCallerAbort = () => true` (client gone) → **neutral** (commit 8abd4b6 preserved). Assert both directions
- [x] 4.4 Adapter→core timing test (clink finding 4) in `core.spec.ts` (or an adapter-level spec) with a fake `HttpClient`, two transports: (i) **no response headers** — adapter first-byte deadline < core first-event deadline, assert the call rejects with a typed `unavailable` `ProviderError` that settles a **trip** and opens the breaker at threshold; (ii) **headers arrive, then no SSE event** — adapter timer disarmed at headers (`http.ts:219`), assert core's first-event timeout still trips. Preserve the existing caller-disconnect-neutral and committed-mid-stream tests

## 5. Definition of done

- [x] 5.1 `npm run build`, `npm run lint`, `npm run typecheck` green; `npm test -w packages/data-plane` (with `REDIS_URL`) + `-w packages/control-plane` green; `npm run test:e2e -w packages/control-plane` green (reap stray jest workers + `redis-cli FLUSHALL` before a clean run)
- [x] 5.2 The E1.2 drain test fails on current `main` and passes after the fix; the E1.3 tests show trip-on-system-timeout AND neutral-on-client-abort; a normal stream is never truncated (existing backpressure test stays green)
- [x] 5.3 Mid-stream commit boundary (invariant 3) unaffected; a changeset added (`npx changeset`, minor — new `PROXY_*` env vars + `/v1` body-error shape change + larger default body limit); `openspec validate fix-proxy-ingress-and-drain --type change --strict --no-interactive` passes
