{
  "name": "@polyrouter/frontend",
  "version": "0.3.0",
  "license": "AGPL-3.0-only",
  "private": true,
  "description": "polyrouter dashboard SPA (SolidJS + Vite)",
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit -p tsconfig.json && vite build",
    "dev": "vite",
    "test": "vitest run",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@polyrouter/shared": "*",
    "solid-js": "^1.9.14",
    "uplot": "^1.6.32"
  },
  "devDependencies": {
    "happy-dom": "^20.10.6",
    "vite": "^8.1.4",
    "vite-plugin-solid": "^2.11.12",
    "vitest": "^4.1.10"
  }
}
